// street
// city
// zipCode
// showAddress(address)

const address = {
  street: "dodekanisou",
  city: "Athens",
  zipCode: 14235,
};

// Factory Function
function showAddress(address) {
  return {
    street,
    city,
    zipCode,
  };
}

showAddress(address);
//Constaction Function

function ShowAddress(address){
  this.street=
}